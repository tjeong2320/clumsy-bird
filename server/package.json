{
  "name": "clumsy-bird-neon-backend",
  "version": "1.0.0",
  "description": "Thin Node/Express backend for Clumsy Bird. Serves the game, creates Neon Hosted Checkout sessions, verifies webhook signatures, and serves a tiny in-memory order ledger so the in-game Shop modal can poll for fulfillment.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}